package com.novikovpashka.projectkeeper.domain.usecases

import android.content.Context
import com.novikovpashka.projectkeeper.data.datafirestore.Project

class AddProjectUseCase {
    fun execute(context: Context, project: Project){
//        ProjectFirestoreRepo.instance!!.addProject(project)
    }
}