package com.example.projectkeeper.domain.usecases

import android.content.Context
import com.example.projectkeeper.data.datafirestore.Project

class DeleteProjectUseCase {
    fun execute(context: Context, project: Project){
//        ProjectFirestoreRepo.instance!!.deleteProject(project)
    }
}